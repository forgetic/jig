[package]
name = "jig-runtime"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared skein runtime bootstrap for jig: a single-threaded reactor runtime and a block_on that hands the body its Cx"

[dependencies]
skein.workspace = true
